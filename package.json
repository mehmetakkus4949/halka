{
  "name": "cember-backend",
  "version": "4.0.0",
  "description": "Çember API - PostgreSQL (pg) ile gerçek, kalıcı veritabanlı backend",
"main": "server.js",
"scripts": {
    "start": "node server.js",
    "seed": "node seed.js"
},
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
