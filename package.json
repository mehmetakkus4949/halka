{
  "name": "cember-backend",
  "version": "3.0.0",
  "description": "Çember API - better-sqlite3 (kuruluysa) veya Node'un yerleşik SQLite'ı ile çalışan gerçek veritabanlı backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node src/seed.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  }
}
