{
  "name": "cember-backend",
  "version": "4.0.0",
  "description": "Çember API - PostgreSQL (pg) ile gerçek, kalıcı veritabanlı backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node src/seed.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
